#include <stdio.h>
int main ()
{
    int a,b,c,d,e;

    scanf("%d %d %d %d %d",&a,&b,&c,&d,&e);

    if (a%2==0)
    {
        printf("%d",a);
    }
    if (b%2==0)
    {
        printf(" %d",b);
    }
    if (c%2==0)
    {
        printf(" %d",c);
    }
    if (d%2==0)
    {
        printf(" %d",d);
    }
    if (e%2==0)
    {
        printf(" %d",e);
    }
    return 0;

}
